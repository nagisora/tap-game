# どうぶつタップ（仮）実装計画

対象読者: 後続の実装エージェント（およびレビューする親）。これは **16ヶ月児向けのおもちゃ** であり、SaaS ではない。本リポジトリは計画のみ。アプリコードはまだ置かない。

必須ターゲット: **Android タブレットの Chrome** を、ホーム画面追加（PWA `display: "standalone"`）し、親が OS の **画面ピン留め** した状態。通常の Chrome タブ内で「住む」前提にしない。

---

## 0. 技術選定（検証結果）

方針は確定とする。後続エージェントは覆さない。

| 層 | 採用 | 不採用（v1） | 理由 |
|---|---|---|---|
| UI | 静的 `index.html` + `styles.css` + `app.js`（バニラ） | Next.js / TanStack Start / SvelteKit / React | 画面は1枚。ルーティング・SSR・コンポーネントツリーが価値を出さない。catnose99 の「1ページのシンプルな Web ページ」行と同じ判断。 |
| ビルド | **なし** | Vite / bundler | 画像約12 + 音声約12 + HTML/CSS/JS 数ファイル。ハッシュ付きファイル名は Service Worker のキャッシュ名で足りる。 |
| ホスト | **Cloudflare Pages（Direct Upload）** | Vercel / 独自サーバ / Pages の Git 連携 | 静的アセット専用。Origin は Cloudflare Pages の Git 連携対象外（GitHub / GitLab のみ）。 |
| インストール | Web App Manifest + 小さな Service Worker | Play ストア、TWA、Capacitor | 親がホーム画面に追加する。 |
| 音声 | Web Audio API + `fetch` した MP3 をデコード | ページロード時の自動再生、複数 `<audio>` の同時再生 | タップ（ユーザジェスチャ）内で `AudioContext.resume()`。短い効果音の排他再生に向く。 |
| オフライン | Service Worker が **全ファイルを install 時に precache** | HTTP キャッシュ任せ、Workbox | 「初回ロード後オフライン」は必須。音声の Range リクエストを SW が中途半端にキャッシュすると壊れるため、音声は SW 経由の通常 GET（200）を Cache Storage に置き、再生は Web Audio 側で行う。 |

### Cloudflare Pages vs Workers static assets（2026）

Cloudflare の新規プロジェクト推奨は Workers + `assets.directory` に寄っている。本プロダクトは **Worker スクリプトも API も不要**。Pages Direct Upload の方が親の手作業（ダッシュボードへのドラッグ＆ドロップ、または `wrangler pages deploy`）が短い。Workers に寄せる利益がゼロなので **Pages に固定**する。後から Worker が必要になったらそのとき移す（v1 では不要）。

### デプロイ経路（Origin がソース・オブ・トゥルース）

1. この Origin リポジトリがソース。
2. Cloudflare Pages の「Git を接続」は使わない（Origin 非対応。一度 Direct Upload にすると Git 連携へ切り替え不可、その逆も不可。最初から Direct Upload を選ぶ）。
3. 実装後の公開手順:
   - `npx wrangler login`
   - `npx wrangler pages project create`（プロダクションブランチ `main`）
   - `npx wrangler pages deploy . --project-name=<name>`（静的ルートをデプロイ。`PLAN.md` 等をサイトに出したくなければ `public/` をルートにしてそこだけデプロイ）
4. GitHub ミラーは作らない（動く部品を増やさない）。
5. カスタムドメインは任意。`*.pages.dev` で受け入れテストは足りる。HTTPS は Pages が付ける。Service Worker / Manifest に必須。

---

## 1. 目的と非目的

### 目的（v1 が成功した状態）

16ヶ月の女の子が Android タブレット上で、**動物の絵をタップするとその鳴き声がすぐ鳴る**。因果が1対1。画面が飽きたら **右向きの大きな三角（▶）1つ** で次のシーンへ。最後のシーンの次は最初。親が一度オンラインで開き、ホーム画面に追加し、ピン留めすれば、その後は機内モードでも遊べる。

### 非目的

- 学習ドリル、クイズ、正解/不正解、スコア、ことばのラベル。
- アカウント、同期、分析、広告、外部リンク、SNS。
- 動物園を増やすこと。動物は約12、シーンは3。
- ネイティブアプリ、Play ストア、課金。
- 子ども向け設定画面（音量・スリープ・ピン留めは **親が OS でやる**。アプリ内 UI にしない）。
- iOS / デスクトップを品質保証すること（壊れなければラッキー。必須テスト対象外）。

### 品質の物差し

見逃しタップ（何も起きない）は、ヒットボックスが少し大きいことより悪い。誤ってシーンが飛ぶのは、戻る矢印があることより悪い。鳴き声が重なってカオスになるのは、前の音を切ることより悪い。

---

## 2. 情報アーキテクチャ

URL は常に1つ（`/` のみ）。シーン切替は **メモリ上のインデックス**。`history.pushState` は使わない（Android の戻るがシーンをチラつかせるため。左矢印を置かないのと同じ理由）。

### シーン（3。4つ目は作らない）

例示の「牧場 / 海 / 森」から、16ヶ月が絵本と生活で知っている鳴き声を優先して **森を「おうちの庭」に差し替えた**。いぬ・ねこを外すと因果の練習にならない。森は v1 では増やさない（第11節）。

順序は固定。ループする。

| 順 | id | 背景の意図 | 動物（4） |
|---|---|---|---|
| 0 | `farm` | 牧場。緑〜黄土のベタ塗り。 | うし / ぶた / にわとり / ひつじ |
| 1 | `yard` | 家の庭。明るい芝と空。 | いぬ / ねこ / あひる / かえる |
| 2 | `sea` | 海。単純な水面と空。深海にしない。 | かもめ / あざらし / ペンギン / イルカ |

シーン名はコードと親向けドキュメント用。**子ども画面には出さない**。

### 動物カタログ（ファイル名 = id）

親が後から差し替えてよい。id とパスを変えなければ JS は触らなくてよい。

| id | 日本語（親用） | シーン | 鳴き声の目安 | 禁止 |
|---|---|---|---|---|
| `cow` | うし | farm | モー、1秒前後 | 長い牧歌BGM |
| `pig` | ぶた | farm | ブー、短い鼻息 | 屠殺・汚い効果音 |
| `chicken` | にわとり | farm | コッコッ（めんどり、0.8〜2秒） | おんどりのコケコッコー、断末魔 |
| `sheep` | ひつじ | farm | メー | 群れの大合唱 |
| `dog` | いぬ | yard | ワン、1〜2回 | 威嚇の低い吠え、持続 |
| `cat` | ねこ | yard | ニャー、短い | シャー（威嚇） |
| `duck` | あひる | yard | ガー | 長い連続 |
| `frog` | かえる | yard | ケロ、1〜2回 | 大音量の夜の大合唱 |
| `seagull` | かもめ | sea | ミャー系の短い鳴き声 | 喧噪のループ |
| `seal` | あざらし | sea | 短い吠え（キャン） | 苦しそうな声 |
| `penguin` | ペンギン | sea | 短いホーン | 金切り |
| `dolphin` | イルカ | sea | キュイ、短いホイッスル | 長いソナー、超音波っぽいノイズ |

合計 **12**。これ以上足さない。差し替え時も「1シーン4匹」を維持する。

### 画面上の要素（子どもが見てよいもの）

1. シーン背景（全面）。
2. 動物イラスト 4 体（通常サイズ。アイコンでも全画面キャラでもない）。
3. 右向き三角 ▶ が **1つ**（次シーン専用）。

それ以外（ロゴ、タイトル、ドットインジケータ、カウンタ、ハンバーガー、ホーム、戻る、文字）は置かない。

### データ形（実装時 `data/scenes.js`）

```js
export const HIT_PADDING_RATIO = 0.06; // 短辺の 6%。下限 48px
export const NEXT_MIN_HIT_PX = 88;

export const scenes = [
  {
    id: "farm",
    background: "#7CB342",
    animals: [
      { id: "cow",     art: "assets/art/farm/cow.svg",     audio: "assets/audio/cow.mp3",     x: 0.18, y: 0.42 },
      { id: "pig",     art: "assets/art/farm/pig.svg",     audio: "assets/audio/pig.mp3",     x: 0.42, y: 0.58 },
      { id: "chicken", art: "assets/art/farm/chicken.svg", audio: "assets/audio/chicken.mp3", x: 0.68, y: 0.38 },
      { id: "sheep",   art: "assets/art/farm/sheep.svg",   audio: "assets/audio/sheep.mp3",   x: 0.78, y: 0.62 },
    ],
  },
  // yard, sea も同型
];
```

`x`, `y` はビューポートに対するイラスト中心の割合（0–1）。実機で重なりと「次へ」との距離を見て数値をいじる。コードのロジックは変えない。

---

## 3. インタラクション仕様

### 3.1 ポインタ

- イベントは **`pointerdown`**（`click` は遅い。300ms やゴーストクリックを避ける）。
- `touch-action: manipulation` を `html, body` に。ダブルタップズーム禁止。
- `contextmenu` / 長押しメニュー / 画像ドラッグ / テキスト選択を無効化。
- 同時多点: v1 は **最初の pointerId だけ**処理。掌が複数ヒットしても二重再生しない（排他再生と併用）。
- `preventDefault` 可能なものはする（スクロール、ピンチ）。

### 3.2 ヒットテスト（シルエットではない）

**しないこと:** 画像の不透明ピクセル、アウトラインから数 px。見逃しが増える。

**すること:** 各動物 DOM の **軸平行バウンディングボックス（`getBoundingClientRect()`）を、全方向に約指1本分だけ膨らませる。**

パディングの計算（実装固定）:

```
short = min(visualViewport.width, visualViewport.height)
pad   = max(48px, 0.06 * short)
expanded = inflate(bbox, pad)
```

10インチ級タブレットの短辺 800 CSS px なら pad ≈ 48px。これが増やしても「指1本」の意図を超えない範囲。

重なり: 膨らませた箱が重なったら、タップ座標から **イラスト中心が近い方**。

「次へ」との重なり: **動物を優先**。「次へ」は動物の expanded box の外にあるときだけ発火。レイアウトで右下（または右辺中央）に置き、動物をそこから離す。

### 3.3 ヒット時

1. その動物を 180ms で `scale(1.00 → 1.08 → 1.00)`（文字・パーティクル・スコアは出さない）。
2. その動物の鳴き声を再生（第4節の排他ルール）。
3. シーンは変えない。

### 3.4 ミス（どの expanded box にも「次へ」にも入らない）

**何もしない。** 効果音なし、フラッシュなし、振動なし。「はずれ」を教えてはいけない。箱を大きくしてミスを減らすのが先。

### 3.5 「次へ」（▶ のみ）

- 見た目: 右向きの正三角形。矢印に見えること。色は背景と高コントラスト（例: 白三角 + 暗い円、または単色の大きな三角）。
- ヒット領域: 三角そのものより大きい。**最小 88×88 CSS px**（できれば 20mm 相当）。透明または薄い円で箱を確保してよい。子どもに「ボタン」と読めるラベルは付けない。
- 左矢印・スワイプ・ドット・メニュー・ホームは **禁止**。スワイプは動物へのリーチと衝突する。
- デバウンス: 発火後 **400ms** は再発火しない（連打でシーンが点滅しない）。
- 動作: `sceneIndex = (sceneIndex + 1) % 3`。クロスフェードするなら **200ms 以下**。ブラウザのスワイプ戻る風アニメは禁止。
- 最終シーンの次はインデックス 0。

### 3.6 レイアウト

- プライマリ: タブレット横向き。セカンダリ: 縦向きも4体が重ならずに入ること（縮小はするがタップ不能にしない）。
- 各動物の描画サイズ: 短い辺の **約 18–28%**（通常サイズ）。
- `100dvh` / `100dvw`、`viewport-fit=cover`。システムジェスチャ用に左右に動物を張り付けすぎない。Android の戻るジェスチャは左端。▶ は右。一致している。
- `overscroll-behavior: none`。プル・トゥ・リフレッシュでシーンがリロードされるのは致命傷。

### 3.7 起動

スプラッシュ・「はじめる」・チュートリアルは置かない。最初の動物タップがオーディオ解錠かつ第一声。初回オンライン時、SW と画像の準備ができるまで短い空白や単色背景は許容。ローディングスピナーや英語の "Loading" は出さない。親がオンラインで一度開き、絵が出てから子どもに渡す（第6・9節）。

---

## 4. 音声

### 4.1 フォーマット

| 項目 | 値 |
|---|---|
| コンテナ | MP3（`.mp3`） |
| サンプリング | 44.1 kHz、**モノラル** |
| ビットレート | 64–96 kbps CBR で足りる |
| 長さ | **0.8–2.0 秒**（にわとりのみ 1.5s 前後まで） |
| ループ | しない |
| ファイルサイズ目安 | 1匹 20–80 KB。12匹でも数 MB 未満 |

必須ターゲットが Android Chrome のみなので AAC 二系統や Opus は持たない。MP3 1本。

### 4.2 ラウドネス（子どもが驚くのを防ぐ）

- 統合ラウドネス **-16 LUFS** 前後（スマホ向けの目安）。タブレット音量は親が決める。
- True peak **≤ -1.5 dBTP**。アタックで 0 dBFS に飛ばない。
- 先頭 10–20ms をフェードインし、デコードノイズのクリックを切る。
- アプリ内音量スライダーは **作らない**。
- ライオン・雷・悲鳴・破裂音は素材段階で捨てる。いぬは「ワン」1–2回。威嚇の低い吠えは使わない。ふくろうを後で足す場合もジャンプスケアの「ホー」は禁止（v1 にふくろうは入れない）。

### 4.3 再生ルール（重複）

**採用: 排他。新しいタップが前の鳴き声を即座に切る。**

理由: 16ヶ月は連打する。重ねると怖い・意味が壊れる。鳴り終わり待ちは「タップしたのに無反応」に聞こえる。

実装:

1. モジュールスコープで `AudioContext` を1つ。最初の `pointerdown` で生成し `resume()`。
2. 各 id の `AudioBuffer` を `fetch` + `decodeAudioData` で持つ（SW キャッシュが効いた通常 GET）。
3. 再生中の `AudioBufferSourceNode` があれば `stop()`。
4. 新しい source を `start()`。
5. `<audio autoplay>` は使わない。ジェスチャなしでは鳴らさない。

`HTMLAudioElement` 1本の `pause(); currentTime=0; play()` でも排他は実現できるが、短い効果音と SW の Range 問題を避けるため **Web Audio を正**とする。

### 4.4 プリロード

- SW が install で全 MP3 を `cache.addAll`。
- JS は起動後（または最初のジェスチャ後）に全バッファをデコード。2秒モノラル PCM × 12 は数 MB。タブレットで問題にならない。
- 未デコードの動物をタップしたら: fetch+decode を待ってから鳴らす（ローカルキャッシュなら短い）。無音のまま終わらせない。デコード失敗時のみミス扱い（何も起きない）。コンソール以外にエラー UI は出さない。

### 4.5 Android Chrome 特有

- 音声付き自動再生はユーザジェスチャか、ホーム画面追加済みオリジンでも **最初の再生はジェスチャに寄せる**（ポリシーに依存しない）。
- `play()` / `resume()` は Promise。`NotAllowedError` は握りつぶして子ども向け UI を出さない（親がタブで開いている失敗モードは第6節）。
- 画面オフ・バックグラウンドで Context が `suspended` になり得る。次の `pointerdown` で `resume()` を毎回呼んでよい。
- iOS Safari のサイレント WAV アンロックは **v1 で書かない**（必須ターゲット外）。`pointerdown` + 単一 Context なら iPad で偶然動く可能性はあるが、受け入れない。

### 4.6 素材の入手（盗用しない）

優先順:

1. 親が録音（近くの動物、安全な距離）。短いクリップに切る。
2. **CC0** の効果音（Freesound 等）。ライセンスファイルを `LICENSES.md` に残す。子ども画面にはクレジットを出さない。
3. 商用ライセンス付き効果音パック（領収と許諾範囲を同ファイルに）。

禁止: アンパンマン・しまじろう等キャラの公式音、YouTube から抜き出したクリップ、権利不明の「効果音まとめ」。

---

## 5. ビジュアル / アート

### 5.1 スタイル

- フラットなイラスト。1匹あたり塗 2–4 色。太い暗いアウトライン（絵の輪郭がそのまま「絵」）。
- 高コントラスト。牧場の緑に緑の虫は置かない。
- 顔は単純。歯列・爪・血・リアルな瞳は禁止。怖くない。
- 背景はベタ塗りまたはごく単純な丘/水面。写真・繁雑な風景は禁止。
- 画面上の文字ゼロ（▶ は図形であって文字ではない）。
- Web フォントを外部から読まない（オフラインと追跡防止）。子ども UI にフォントは不要。

### 5.2 サイズと配置

- 動物は「普通サイズ」。指で押しやすいが、1匹が画面を占領しない。
- 4体の expanded hit box が画面の大半を覆ってよい（それが目的）。ただし ▶ のヒット領域は空ける。
- SVG 推奨（拡大してもギザギザにならない、ファイルが小さい、透過が簡単）。生成結果が PNG なら最長辺 1024px、背景透過、`image-rendering` は auto。

### 5.3 入手経路（盗用しない）

優先順:

1. **新規生成**（画像モデルや手描き）→ 親が「怖い/気持ち悪い/既存キャラに似ている」を見て差し替え。
2. 発注（単純なフラットアイコンの illustrator）。
3. 明示的に商用利用可のストック。ライセンスを `LICENSES.md` に保存。

禁止: 絵本・アニメ・キャラクターのトレース、検索画像の右クリック保存、他アプリのスプライト。

実装エージェントは、生成する場合でも **著作権のあるキャラに寄せない**プロンプトにする（「アンパンマン風」等は禁止）。レビュー待ちのプレースホルダは単色シルエット + 親用 id コメントでよいが、子どもに渡すビルドには入れない。

### 5.4 PWA アイコン

- 192px と 512px の PNG。`purpose: "any maskable"` 用にセーフゾーンを取る。
- 中身は動物1匹（いぬまたはうし）+ 無地。文字なし。

---

## 6. フルスクリーン / PWA / Android ピン留め

アプリはブラウザ UI を消さない。**親の手順**が消す。ここを実装で「頑張らない」。

### 6.1 アプリ側で用意するもの

`manifest.webmanifest`:

- `name`: 親がホーム画面で識別できる短い日本語（例: 「どうぶつ」）。`short_name` も短く。
- `start_url`: `./`
- `scope`: `./`
- `display`: **`"standalone"`**（指定どおり。`fullscreen` はステータスバーまで消し、親が時刻・電池を見失う。使わない）。
- `background_color` / `theme_color`: 明るい中性色。
- `orientation`: `"any"`
- `icons`: 192 / 512。
- `id` を同一オリジンで安定させる（再インストールでアイコンが分裂しないように）。

HTML:

- `<link rel="manifest" href="./manifest.webmanifest">`
- viewport: `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover`
- テーマカラー meta。

Service Worker: 第8節。インストール可能性のためにも fetch ハンドラを持つ。

**検出:** `window.matchMedia("(display-mode: standalone)")`。タブで開いているとき子ども向けバナーは出さない（文字が読める UI を子どもに見せない）。親向け手順は `PARENT.md`（実装時に PLAN から抜粋して作る）。

### 6.2 親の手順（受け入れの一部）

タブレットは Android。Chrome。機種で文言が少し違う。

**A. 一度オンラインで開く**

1. 充電する。音量は中央より少し下（後で上げられる）。
2. Chrome で Pages の URL を開く。動物の絵が4体見えるまで待つ（初回キャッシュ）。
3. 鳴るか、自分で1回タップして確認。

**B. ホーム画面に追加（タブから出る）**

1. Chrome 右上 ⋮ → **「ホーム画面に追加」** または **「アプリをインストール」**。
2. 名前を確認して追加。
3. **Chrome のタブは閉じる。**
4. ホーム画面のアイコンから起動する。
5. 成功条件: **アドレスバーが無い。タブ帯が無い。** これが standalone。

**C. 画面ピン留め（子どもがアプリから出られないようにする）**

ピン留めは Chrome の「タブを固定」ではない。**OS の画面ピン留め / アプリのピン留め**。

1. 設定 → セキュリティ（とプライバシー）→ 詳細 / その他のセキュリティ → **アプリのピン留め / 画面のピン留め** を ON。
2. **「ピン留めを解除する前に画面ロックを要求」を ON**（オフだと子どもが解除できる）。端末に PIN / パターン / 指紋があること。
3. PWA（ホーム画面アイコンから開いた方）を前面にする。
4. 概要画面（履歴）を開く:
   - 3ボタン: □（履歴）
   - ジェスチャ: 下から上へスワイプして止める
5. そのカードの **アプリアイコン** → **ピン留め / 固定** → 確認。
6. 子どもに渡す。

**D. 解除（親だけ）**

- 3ボタン: **戻る + 履歴を同時長押し** → ロック認証。
- ジェスチャ: **下端から上へスワイプしたまま長押し** → ロック認証。
- メーカー差: Pixel / Galaxy / Xperia / AQUOS でメニュー名が違う。設定内を「ピン」で検索すれば足りることが多い。

**E. スリープと音**

- ディスプレイのスリープを、遊ぶ長さより長くする。または充電しながら「充電中はオフにしない」系があれば使う。
- マナーモード / Do Not Disturb は任意。着信でピン留めが通話画面に飛ぶ機種がある（第11節）。
- 音量ボタンはピン留め中も効くことが多い。渡す前に親が決める。

### 6.3 失敗モード（アプリでは直らない。親手順で防ぐ）

| 症状 | 原因 | 対処 |
|---|---|---|
| 上にアドレスバー | Chrome タブで遊んでいる | ホーム画面アイコンから開き直す |
| 戻るで Chrome の前のページ / アプリが終わる | タブ利用、または PWA でも履歴が空でシステムバックが終了 | ピン留め。実装は `pushState` しない（シーンが戻る方が悪い） |
| 他タブ・ホームに行く | ピン留めしていない | C をやり直す |
| 画面が黒い / 寝る | スリープ | E |
| 音が出ない | サイレント、音量0、ジェスチャ前、タブがバックグラウンド | 音量、前面、1回タップ |
| 下からリロードされる | プル・トゥ・リフレッシュ | 実装で overscroll 禁止。ピン留めでも確認 |
| ピン留めできない | 設定がオフ、概要画面の操作ミス | C-1 |
| 子どもがピン留め解除 | 解除時ロックがオフ | C-2 を必須 |
| 「インストール」が出ない | HTTP、manifest 壊れ、アイコン無し | HTTPS + manifest 検証 |
| Chrome のタブピン留めと混同 | Chrome 144+ のタブ固定 | OS の画面ピン留めを使う。タブ固定ではホームに逃げられる |

実装チェックリスト: ズーム禁止、選択禁止、overscroll 禁止、standalone でアドレスバーが消えること、ピン留めは文書化のみ。

---

## 7. 将来リポジトリのファイル配置

計画時点では本ファイルと `SUMMARY.md` / `README.md` のみ。実装時は次に **これ以上増やさない**。

```
/
  PLAN.md                 # 本ファイル（残す）
  SUMMARY.md
  README.md               # 実行方法。親向け短い入口
  PARENT.md               # 実装時: 第6節を親向けに抜き出したもの
  LICENSES.md             # 絵と音の許諾。子ども画面には出さない
  public/                 # Cloudflare に上げるルート（推奨。md を配信しない）
    index.html
    styles.css
    app.js
    sw.js
    manifest.webmanifest
    assets/
      art/farm/{cow,pig,chicken,sheep}.svg
      art/yard/{dog,cat,duck,frog}.svg
      art/sea/{seagull,seal,penguin,dolphin}.svg
      audio/{id}.mp3
      icons/icon-192.png
      icons/icon-512.png
    data/
      scenes.js           # 第2節のカタログ。ES module
  wrangler.toml           # 任意。pages deploy の project name だけでも可
```

`package.json` は必須にしない。ローカル確認は `python -m http.server` や `npx serve public`（SW は localhost で動く。`file://` は使わない）。

`index.html` はシェルのみ。シーンは JS が `scenes.js` から描画。動物は `<img>` またはインライン SVG。ヒットは DOM 矩形。canvas 1枚に全部描く方式はヒットを自前計算する必要があり、v1 ではやらない。

---

## 8. オフラインとホスティング

### 8.1 Service Worker（必要。省略しない）

「初回のあとオフライン」は HTTP キャッシュでは足りない（追い出し、音声 Range、リロード時に `index.html` が取れない）。

仕様:

- `sw.js` は自前の短いファイル。Workbox も CDN も使わない（オフラインと外部接続禁止）。
- `CACHE = 'animal-tap-v1'`。アセットを足したら **v2 にリネーム**。
- `install`: `cache.addAll(PRECACHE)`。リストは `index.html`, CSS, JS, manifest, 全 SVG/PNG, 全 MP3, アイコン。1件でも失敗したら install 失敗（欠けた鳴き声よりマシ）。
- `activate`: 古い `animal-tap-*` を削除。`clients.claim()`。
- `fetch`: same-origin は **Cache falling back to network**。ナビ（document）もキャッシュ優先（オフライン起動のため）。
- 音声リクエストを SW が 206 のまま保存しない。precache は Range なしの GET。ページ側は `fetch(url)`（SW が 200 を返す）→ `arrayBuffer()` → `decodeAudioData`。
- 更新: 親がオンラインで開き直せば新 SW が取れる。子ども利用中に勝手にリロードしない（`skipWaiting` は install 後でよいが、`location.reload` はしない）。

### 8.2 ネットワークポリシー

- 外部オリジンへのリクエスト禁止（フォント、アナリティクス、CDN 上の Workbox、OGP クローラ用の遠隔画像）。
- CSP を `index.html` に: `default-src 'self'; media-src 'self'; img-src 'self'; connect-src 'self'; worker-src 'self'`。インラインを使うならその場で `'unsafe-inline'` を最小に。できれば CSS/JS は外部ファイル。
- 広告・トラッキング・アカウント・リンクなし。`<a target=_blank>` なし。

### 8.3 ホスティング手順（実装エージェント向け）

1. `public/` に静的ファイルを置く。
2. Cloudflare アカウント（親または実装者）。無料枠で足りる。
3. Direct Upload プロジェクトを作成（Git に接続しない）。
4. `npx wrangler pages deploy public --project-name=toddler-animal-tap`
5. 出た `https://<name>.pages.dev` をタブレットの Chrome で開く。
6. 第9節を実施。

Headers（任意だが推奨）: `Cache-Control` は HTML 短め、ハッシュ無しアセットは SW が本体。Pages の `_headers` で `service-worker` の MIME はデフォルトでよい。

---

## 9. 受け入れテスト（親がタブレットで5分）

オンラインで PWA 化・ピン留めしたあと、できれば機内モードで。失敗したら渡さない。

1. **ホーム画面アイコンから開く。** アドレスバーが無い。
2. **牧場でうしをタップ → モーがすぐ鳴る。** 絵が少し弾む。
3. **うしの輪郭のすぐ外側（指1本ぶん）をタップ → まだ鳴る。** 見逃しが無いこと。
4. **空の背景（どの動物からも離れた場所）をタップ → 何も起きない。** シーンも変わらない。
5. **連打。** 前の声が切れ、最後のタップの動物だけが聞こえる。重ならない。
6. **▶ を1回 → 庭。** いぬまたはねこが鳴る。
7. **▶ をもう1回 → 海。** もう1回 → 牧場に戻る。左矢印が無い。スワイプでシーンが変わらない。
8. **ピン留め中にホームや概要を出そうとしても出られない。**
9. **機内モード（または Wi-Fi/モバイルオフ）で PWA をいったん閉じてアイコンから再起動。** 絵と音が残る。
10. **音量が親の想定内。** びっくりして端末を放り投げない。怖くない。

この10が通れば v1 完了。自動 E2E は必須にしない（実機のピン留めとスピーカはエミュレータで代替できない）。デスクトップ Chrome でのヒットボックス確認は開発中のみ。

---

## 10. v1 のスコープ外

後続エージェントは、依頼されても v1 ブランチに混ぜない。

- Play ストア / TWA / Capacitor / Flutter。
- アカウント、クラウドセーブ、家族共有。
- 動物の追加、第4シーン（森）、言語切替、名前ラベル。
- 左（戻る）矢印、スワイプ、メニュー、ホームボタン、シーンインジケータ。
- 子ども向け設定 UI（音量、効果オンオフ、スリープ）。
- クイズ、ごほうび、アニメの長いストーリー。
- iOS 品質保証、デスクトップ PWA 品質保証。
- プッシュ通知、共有シート、外部リンク。
- フレームワークへの「将来の拡張のため」の導入。

---

## 11. リスク

| リスク | 深刻度 | 緩和（v1 でやること） |
|---|---|---|
| 自動再生ブロックで無音 | 高 | 再生は `pointerdown` 内のみ。Context をその場で `resume()`。親が初回に1回鳴らしてから渡す |
| タブのまま遊んでアドレスバー・戻る・他タブ | 高 | PWA standalone + 親手順。タブ向けの子ども UI は作らない |
| ピン留め忘れ / 解除時ロックオフ | 高 | `PARENT.md` に必須と書く。アプリでキオスク化はできない |
| スリープで画面が落ちる | 中 | 親がタイムアウトと充電を設定。Wake Lock API は許可プロンプトと切れやすさがあるので **v1 では使わない** |
| 大きすぎる / 怖い声 | 高 | LUFS 正規化、素材レビュー、猛獣なし |
| ライセンス侵害 | 高 | 生成 or CC0 or 購入。トレース禁止。`LICENSES.md` |
| SW が音声を 206 で壊す | 中 | Web Audio + precache 200。Workbox Range に頼らない |
| プル・トゥ・リフレッシュ | 中 | `overscroll-behavior: none`、ピンチズームオフ |
| システムバックで PWA 終了 | 中 | ピン留め。`pushState` ハックはしない（シーンが戻る副作用） |
| 着信でピン留めが通話に飛ぶ | 低 | 親が機内モードか通知制限。実装では不可 |
| ヒットが小さすぎて何も起きない | 高 | bbox + 指幅。ミスより甘い箱 |
| ▶ 誤タップでシーン点滅 | 中 | 動物優先、400ms デバウンス、左矢印なし |
| Cloudflare に Origin を Git 接続しようとする | 低 | Direct Upload のみ、と本計画に書いた |
| フレームワーク導入の誘惑 | 低 | 第0節。PR で Next を足したら戻す |

---

## 12. 実装順（後続エージェント）

レビュー済みの本計画以外のプロダクト質問で止めない。素材の味（絵が好みか）だけ親確認。

1. `public/index.html` + CSS: 全画面、ズーム/選択/overscroll 禁止、牧場に仮の4矩形。
2. `pointerdown` ヒット（bbox + pad）+ Web Audio でダミー1音。
3. `scenes.js` どおり12体。仮 SVG（単色でも可）と正規化済み MP3。
4. ▶、ループ、デバウンス、動物優先ヒット。
5. Manifest + `sw.js` precache。`file://` で試さない。
6. `wrangler pages deploy`。Android タブレットで第9節。
7. `PARENT.md` を第6節から作成。コミット。

コミットは小さく。おもちゃのコードにアカウントやルータを足さない。

---

## 13. 未決でよいこと（レビュー通過条件）

プロダクトとして閉じていないのは **絵と音の味** だけ。

- うしを少し丸くするか等は親が差し替え。
- シーン背景の正確な色。
- Pages プロジェクト名と、`*.pages.dev` か独自ドメインか。

ヒットの定義、シーン数、動物 id、次へが1方向、排他再生、スタック、オフライン、ピン留め、非ゴールは **未決にしない**。
